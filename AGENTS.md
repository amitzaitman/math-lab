# Working in Math Lab

- Node 24 and npm. Commit a generated package-lock.json and use npm ci once present.
- Run npm run verify; report checks that could not run.
- Keep mathematical state independent of rendering/animation.
- Preserve explicit numerator/denominator representation.
- Keep activity-specific state local; share code after a second real use case.
- Support touch, keyboard, pointer cancellation, reduced motion.
- Hebrew layout is RTL; math axes and bars remain LTR.
- No generated dist in Git. No backend or analytics without a product need.
- Test offline/update behavior against production output.
